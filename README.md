
This project demonstrates a basic SQL Injection vulnerability using Damn Vulnerable Web Application (DVWA).

SQL Injection occurs when user input is not properly sanitized and is directly used in SQL queries.

1' OR '1'='1

Using the above payload, all user records were displayed instead of a single user, proving the SQL Injection vulnerability.

- `sql_injection_exploit.sh`
- `screenshots/payload.png`
- `screenshots/output.png`

- DVWA
- Apache
- MySQL
- Ubuntu Linux

Task completed successfully.

